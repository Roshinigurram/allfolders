using System;
using System.Collections.Generic;
using System.IO;
using System.Text;

namespace task2
{
    class directoryGet
    {
        public void getFullDirectories()
        {
        //    string rootdir = Console.ReadLine();
         
        //    // get list of files in root directory and all its subdirectories
        //    string[] files = Directory.GetFiles(rootdir, "*", SearchOption.AllDirectories);
        //    Console.WriteLine(String.Join(Environment.NewLine, files,sizeof );
        //Console.WriteLine(String.Join(Environment.NewLine, files.Length));
 
        //// get list of directories and subdirectories
        //string[] dirs = Directory.GetDirectories(rootdir, "*", SearchOption.AllDirectories);
        //    Console.WriteLine(String.Join(Environment.NewLine, dirs));
        //    Console.WriteLine(String.Join(Environment.NewLine, dirs.Length));
            

        }
    }
}
